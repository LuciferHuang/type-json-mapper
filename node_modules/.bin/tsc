#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_typescript@3.9.7@typescript/bin/tsc" "$@"
  ret=$?
else 
  node  "$basedir/../_typescript@3.9.7@typescript/bin/tsc" "$@"
  ret=$?
fi
exit $ret
